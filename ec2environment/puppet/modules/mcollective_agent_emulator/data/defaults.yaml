lookup_options:
  mcollective_agent_emulator::gem_dependencies:
    merge:
      strategy: deep
  mcollective_agent_emulator::package_dependencies:
    merge:
      strategy: deep
  mcollective_agent_emulator::config:
    merge:
      strategy: deep
  mcollective_agent_emulator::client_config:
    merge:
      strategy: deep
  mcollective_agent_emulator::server_config:
    merge:
      strategy: deep
